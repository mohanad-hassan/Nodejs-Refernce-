// Create Nodejs Server 
var http = require('http'); // 1 - Import Node.js core module

var server = http.createServer(function (req, res) {   // 2 - creating server

    //handle incomming requests here..
these code will excute when the user requests your website ,
procces.exit() to turn off the server . off course you dont do that here.
Send Respone 
  res.write('<html>')
   res.write('<p> i love you <p/>')
   res.write('</p>')
   
});

server.listen(5000); //3 - listen for any incoming requests

console.log('Node.js web server at port 5000 is running..').

//ctrl + c to shut down the server .
//cls to clear the console .
//
